pipeline {
    agent any
    tools {
        maven 'maven'
    }
    stages {
       
        stage('deploy') {
          steps{
          
            sh(returnStdout: true, script: "git tag --sort version:refname | tail -1").trim()

    }
}
}
}

pipeline {
    agent any

    stages {
       
        stage('deploy') {
          steps{
            script{
            def TAG_NAME = binding.variables.get("TAG_NAME")
            if (TAG_NAME != null) {
            sh "echo $TAG_NAME"
            } else {
            sh "echo Non-tag build"
}
          
           }

    }
}
}
}

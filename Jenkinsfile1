pipeline {
    agent any
    tools {
        terraform 'terraform'
    }
    stages {
        stage('deploy') {
          steps{
          sh 'terraform init'
           script {
             TAG = sh(returnStdout: true, script:'git describe --abbrev=0').trim()
             echo "${TAG}"
            }
           }
    }
}
}

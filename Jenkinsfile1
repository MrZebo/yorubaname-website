pipeline {
    agent any
    stages {
        stage('deploy') {
          steps{
           script {
             latestTag = sh 'git describe --abbrev=0'
             echo $latestTag
            }
           }

    }
}
}
}
